def minimum(arr):
    """Esta función recibe una lista de enteros como entrada y devuelve el número más pequeño de esa lista."""
    return min(arr)

def maximum(arr):
    """Esta función recibe una lista de enteros como entrada y devuelve el número más grande de esa lista."""
    return max(arr)
